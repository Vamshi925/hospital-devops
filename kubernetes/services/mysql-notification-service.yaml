apiVersion: v1
kind: Service

metadata:
  name: mysql-notification
  namespace: hospital

spec:
  clusterIP: None

  selector:
    app: mysql-notification

  ports:
  - port: 3306
    targetPort: 3306
