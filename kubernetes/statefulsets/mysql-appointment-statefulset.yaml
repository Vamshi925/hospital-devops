apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mysql-appointment
  namespace: hospital

spec:
  serviceName: mysql-appointment

  replicas: 1

  selector:
    matchLabels:
      app: mysql-appointment

  template:
    metadata:
      labels:
        app: mysql-appointment

    spec:
      containers:
      - name: mysql

        image: mysql:8.4

        ports:
        - containerPort: 3306

        env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-appointment-secret
              key: MYSQL_ROOT_PASSWORD

        - name: MYSQL_DATABASE
          value: AppointmentService

        volumeMounts:
        - name: mysql-storage
          mountPath: /var/lib/mysql

  volumeClaimTemplates:
  - metadata:
      name: mysql-storage

    spec:
      accessModes:
      - ReadWriteOnce

      resources:
        requests:
          storage: 5Gi
