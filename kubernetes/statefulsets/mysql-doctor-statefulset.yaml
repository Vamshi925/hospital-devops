apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mysql-doctor
  namespace: hospital

spec:
  serviceName: mysql-doctor

  replicas: 1

  selector:
    matchLabels:
      app: mysql-doctor

  template:
    metadata:
      labels:
        app: mysql-doctor

    spec:
      containers:
      - name: mysql

        image: mysql:8.4

        ports:
        - containerPort: 3306

        env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-doctor-secret
              key: MYSQL_ROOT_PASSWORD

        - name: MYSQL_DATABASE
          value: DoctorService

        volumeMounts:
        - name: mysql-storage
          mountPath: /var/lib/mysql

  volumeClaimTemplates:
  - metadata:
      name: mysql-storage

    spec:
      accessModes:
      - ReadWriteOnce

      resources:
        requests:
          storage: 5Gi
