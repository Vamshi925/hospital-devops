apiVersion: v1
kind: Service

metadata:
  name: frontend
  namespace: {{ .Values.namespace }}

  labels:
{{ include "hospital.labels" . | nindent 4 }}

spec:
  selector:
    app: frontend

  ports:
    - port: 80
      targetPort: 80

  type: {{ .Values.frontend.service.type }}
